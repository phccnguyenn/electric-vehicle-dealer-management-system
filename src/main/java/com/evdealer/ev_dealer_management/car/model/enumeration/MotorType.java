package com.evdealer.ev_dealer_management.car.model.enumeration;

public enum MotorType {
    AC_INDUCTION,       // Động cơ cảm ứng AC (Tesla sử dụng)
    PERMANENT_MAGNET,   // Động cơ nam châm vĩnh cửu (Permanent Magnet)
    DC_BRUSHED,         // Động cơ DC chổi than
    DC_BRUSHLESS,       // Động cơ DC không chổi than
    SYNCHRONOUS
}
