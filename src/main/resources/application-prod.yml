server:
  port: ${SERVER_PORT}
  servlet:
    context-path: /evdealer

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SPRING_MAIL_USERNAME}
    password: ${SPRING_MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        debug: false
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  application:
    name: ev-dealer-management
  main:
    allow-bean-definition-overriding: true
  datasource:
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://${MSSQL_ADDRESS};databaseName=${MSSQL_DB};encrypt=true;trustServerCertificate=true
    username: ${MSSQL_SA_USERNAME:sa}
    password: ${MSSQL_SA_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION} # 1 ngày
  refresh-expiration: ${JWT_REFRESH_EXPIRATION} # 7 ngày

file:
  directory: ${FILE_UPLOAD_DIR:}
  contract: ${FILE_CONTRACT_DIR:}
